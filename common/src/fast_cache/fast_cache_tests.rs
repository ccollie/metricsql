

// fn test_cache_reset_update_stats_set_concurrent() {
//     let c = FastCache::new(12334);
//
// // run workers for update cache stats
// (0 .. 100).par_iter(|i| {
//  let s: Stats
//  for {
// c.update_stats(&s)
// }
// }
// }()
// }
//
// // run workers for setting data to cache
// (0 .. 100).par_iter(|i| {
//     let key = format!("key_{}", j).as_bytes();
//     let value = format!("value_{}", j).as_bytes();
//     c.set(key, value);
// })
//
// }
